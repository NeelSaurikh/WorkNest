<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WorkNest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WorkNest" parent="Base.Theme.WorkNest" />

    <style name="AppTheme.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your splash theme here -->
        <item name="android:windowBackground">@color/white</item> <!-- Or your splash bg color -->
    </style>


    <style name="Theme.WorkNest.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <style name="Theme.WorkNest.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>